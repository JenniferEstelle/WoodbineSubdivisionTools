//left in progress

GarbageDayModule.factory('CalculateGarbageDay', CalculateGarbageDay); 

CalculateGarbageDay.$inject = [''];
function CalculateGarbageDay () {
  var service = this;

  var holidays;
  // factory function body that constructs shinyNewServiceInstance
  service.getHolidays = function (holidays) {
      holidays = {

      }
  }
  return shinyNewServiceInstance;
});